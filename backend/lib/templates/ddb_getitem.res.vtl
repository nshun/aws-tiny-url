#set($long_url = $input.path('$.Item.long_url.S'))
#if(!$long_url.isEmpty())
    #set($context.responseOverride.status = 302)
    #set($context.responseOverride.header.location = $long_url)
#else
    #set($context.responseOverride.status = 404)
#end
